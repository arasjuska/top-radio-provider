{
  "name": "top-radio-provider",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Verified, quality-first stream provider for Top Radio Pro",
  "scripts": {
    "build": "node scripts/build-provider.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
