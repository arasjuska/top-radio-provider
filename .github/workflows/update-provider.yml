name: Update verified streams

on:
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'
  push:
    paths:
      - 'data/**'
      - 'scripts/**'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/update-provider.yml'

permissions:
  contents: write

concurrency:
  group: provider-refresh
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - name: Checkout global IPRD catalog
        uses: actions/checkout@v4
        with:
          repository: iprd-org/iprd
          path: vendor/iprd
          sparse-checkout: streams
          sparse-checkout-cone-mode: true
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test provider logic
        run: npm test
      - name: Validate streams and build provider
        run: npm run build
      - name: Sanitize HLS audio metadata
        run: node scripts/sanitize-provider.mjs
      - name: Enrich station logos and metadata
        run: node scripts/enrich-logos.mjs
      - name: Build global sharded search catalog
        run: node scripts/build-global-catalog.mjs
      - name: Build country catalog indexes
        run: node scripts/build-country-index.mjs
      - name: Commit refreshed provider data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/stations.json dist/status.json dist/catalog
          if git diff --cached --quiet; then
            echo "Provider data unchanged"
            exit 0
          fi
          git commit -m "chore: refresh verified radio streams and global catalog"
          git pull --rebase origin master
          git push origin master
